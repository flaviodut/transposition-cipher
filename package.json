{
  "name": "transposition-cipher-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "sass": "^1.69.7",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
